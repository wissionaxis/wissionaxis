export interface LinksType{
    name:string,
    linked:string
} 

export interface SectionType{
    id:string,
    title:string,
    description:string
}

export interface Section {
  heading: string;
  description: string;
  icon: React.ReactNode;
}

